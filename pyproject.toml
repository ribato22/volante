# pyproject.toml
[project]
name = "volante"
dynamic = ["version"]
description = "Volante — a transparent, user-owned model router and orchestration control plane."
readme = "README.md"
# 3.11.10, not 3.11: the ipaddress is_private/is_global fix (gh-113171) landed
# there, and tools/fetch_url.py leans on those properties to refuse internal
# addresses. On 3.11.0-3.11.9 that check is subtly wrong, so the floor is a
# security boundary rather than a packaging preference.
requires-python = ">=3.11.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "ribato" }]
keywords = [
    "llm",
    "llm-router",
    "model-routing",
    "ai-orchestration",
    "multi-agent",
    "agentic",
    "supervisor",
    "routing",
    "anthropic",
    "openai",
    "mcp",
    "evaluation",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    # Honest, not aspirational: the subprocess sandbox and CLI-agent providers use
    # os.setsid/killpg (POSIX-only). The pure-Python library core (Runtime, Router,
    # Registry, etc., minus volante.tools.sandbox / cli_agent) is importable cross-platform.
    "Operating System :: POSIX",
    "Environment :: Console",
    "Framework :: AsyncIO",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Typing :: Typed",
]
dependencies = [
    "anthropic>=0.40",
    "openai>=1.50",
    "httpx>=0.27",     # direct use in the host-mediated fetch_url tool
]

[project.optional-dependencies]
# Web UI (webui/): `uv sync --extra ui` then `uv run python -m webui`.
ui = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
]
# MCP server (volante_mcp/): `uv sync --extra mcp` then `uv run python -m volante_mcp`.
# Exposes Volante as a tool to IDE AI agents (Claude Code, Cursor, VS Code, Windsurf).
mcp = [
    # Upper bound is load-bearing, not caution: mcp 2.0.0 (2026-07-28) removed
    # mcp.server.fastmcp, which volante_mcp imports, so an unbounded range broke
    # EVERY published install path the day it landed — uvx, MCPB, the plugin,
    # Smithery and the registry record all resolve the newest release. uv.lock
    # pinned an older mcp, so neither CI nor local runs could see it.
    "mcp>=1.2,<3",
]

[project.scripts]
volante = "volante.cli:main"
# MCP server for IDE AI agents; needs the `mcp` extra at runtime.
volante-mcp = "volante_mcp:main"

# PyPI distribution name is `volante` (plain `volante` is taken); the
# importable package and the CLI command are both still `volante`.
[project.urls]
Homepage = "https://github.com/ribato22/volante"
Repository = "https://github.com/ribato22/volante"
Issues = "https://github.com/ribato22/volante/issues"
Changelog = "https://github.com/ribato22/volante/blob/main/CHANGELOG.md"
Documentation = "https://github.com/ribato22/volante#readme"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "mypy>=1.11",
    "fastapi>=0.110",   # webui tests
    "uvicorn>=0.29",
    "mcp>=1.2,<3",      # volante_mcp tests
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "src/volante/__init__.py"

[tool.hatch.build.targets.wheel]
# `volante` is the engine/CLI; `volante_mcp` is the installable MCP server (its `mcp`
# dependency is opt-in via the `mcp` extra). The Web UI (webui/) stays source-only.
packages = ["src/volante", "volante_mcp"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-m 'not integration'"
markers = [
    "integration: touches the real network; skipped by default",
]
filterwarnings = [
    # starlette's TestClient deprecating httpx in favour of httpx2 (test-only, upstream).
    "ignore:Using .httpx. with .starlette.testclient. is deprecated",
]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests", "eval", "webui", "volante_mcp"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
# webui/app.py is a single self-contained HTML/CSS/JS document embedded as one
# string literal (no build step, no external assets). Inline SVG path data and
# CSS declarations legitimately run past the line limit; wrapping them would hurt
# readability without any code-quality gain. Line-length is not enforced here.
"webui/app.py" = ["E501"]
# eval/tasks_text.py holds prompt text VERBATIM — the exact string a model is sent,
# line breaks included. The model sees those breaks, so rewrapping them to satisfy a
# linter would change the measurement, and a goal that drifts from the artifact it
# was validated against is worse than a long line.
"eval/tasks_text.py" = ["E501"]

[tool.mypy]
# Pragmatic lenient baseline (deliberately NOT --strict): enforces the shipped
# py.typed promise (src/volante/py.typed) without a large upfront annotation
# project. mypy defaults + ignore_missing_imports, scoped to the library only
# (not tests/eval/webui/examples).
python_version = "3.11"
ignore_missing_imports = true
files = ["src/volante"]
